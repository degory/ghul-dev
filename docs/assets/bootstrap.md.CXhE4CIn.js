import{_ as t,a,b as i,c as o,d as r,e as n}from"./chunks/bootstrap-ghūl-self-hosting-transpiler.drawio.WkZHwCzJ.js";import{_ as l,I as s,g as p,j as h}from"./chunks/framework.CbuNJfvM.js";const c="/bootstrap-all-transpilers.drawio.svg",y=JSON.parse('{"title":"history","description":"","frontmatter":{},"headers":[],"relativePath":"bootstrap.md","filePath":"bootstrap.md"}'),g={name:"bootstrap.md"};function d(m,e,u,b,f,w){return s(),p("div",null,[...e[0]||(e[0]=[h('<h1 id="history" tabindex="-1">history <a class="header-anchor" href="#history" aria-label="Permalink to &quot;history&quot;">​</a></h1><h2 id="background" tabindex="-1">background <a class="header-anchor" href="#background" aria-label="Permalink to &quot;background&quot;">​</a></h2><p>The ghūl <a href="https://github.com/degory/ghul" target="_blank" rel="noreferrer">compiler</a> is a <a href="https://en.wikipedia.org/wiki/Self-hosting_(compilers)" target="_blank" rel="noreferrer">self-hosting compiler</a>: it can compile itself from its own source code. Developing the self-hosting ghūl compiler required an initial <a href="https://en.wikipedia.org/wiki/Bootstrapping_(compilers)" target="_blank" rel="noreferrer">bootstrap</a>. Bootstrapping a new compiler for a new language is a classic chicken-and-egg problem: you need a compiler to compile your new compiler, but that compiler doesn’t exist yet.</p><p>The solution is to write the initial compiler in an existing language. Once that compiler is sufficiently reliable, its source code must be somehow translated into the new language. This results in a compiler that can compile the new language including its own source code.</p><p>The discussions on bootstrapping use <a href="https://en.wikipedia.org/wiki/Tombstone_diagram" target="_blank" rel="noreferrer">tombstone diagrams</a>. In a T shaped tombstone diagram, the T represents a particular version of a compiler. Within the T, three languages are named: the source language, the target language and the implementation language. The target language is typically some kind of IL or machine language, but for a transpiler the target language is source code.</p><p>For example a compiler C that reads source in language S, translates it to language T, and is implemented in language I, would be depicted like this:</p><p><img src="'+t+'" alt="Example tombstone showing compiler for language S, targeting language T, written in language I"></p><p>To use compiler C, we need a compiler for source language I, and a machine to execute target code T. Provided we have those, we can compile C with the compiler for I, and when we run the resulting executable, it will consume source code written in S, and produce target code that will execute on T. If the compiler for I won&#39;t run on the machine that executes T, then compiler C is a <a href="https://en.wikipedia.org/wiki/Cross_compiler" target="_blank" rel="noreferrer">cross compiler</a></p><h2 id="" tabindex="-1"><a class="header-anchor" href="#" aria-label="Permalink to &quot;&quot;">​</a></h2><p>I wrote the first version of the ghūl compiler in the L programming language - a language I designed and implemented a self-hosting compiler for previously.</p><p>Getting to this point required <a href="https://en.wikipedia.org/wiki/Bootstrapping_(compilers)" target="_blank" rel="noreferrer">bootstrapping</a> the compiler: bootstrapping is the process of getting</p><p>Bootstrapping it now is routine: it&#39;s done by the CI/CD pipeline on every PR merge and release. However, the initial bootstrap was much more complex.</p><p>The initial bootstrap of a self-hosting compiler for a new programming language is a complex and challenging task due to the paradoxical nature of the process: the compiler, which translates code written in the new language into executable machine code, needs to be written in the new language it&#39;s supposed to compile, but no compiler exists that can compile it. This creates a &quot;chicken-and-egg&quot; problem where the first version of the compiler must be written in a different, already established language. Bootstrapping is the process of taking this initial non-self-hosting compiler and somehow translating it into a new compiler written in the new language and capable of compiling itself.</p><p>The initial bootstrap of ghūl involved several stages and I&#39;ll cover it in detail below.</p><h2 id="bootstrapping-the-l-compiler" tabindex="-1">bootstrapping the L compiler <a class="header-anchor" href="#bootstrapping-the-l-compiler" aria-label="Permalink to &quot;bootstrapping the L compiler&quot;">​</a></h2><p>The original basis for the compiler was written many years ago. I was a compiler for a language quite different from ghūl - a language I called L. This L compiler was originally written in C++, and compiled the L language to x64 assembly language. I manually bootstrapped the L compiler by hand translating its C++ source code into L. This was made easier (if no less tedious) by carefully sticking to an L-compatible subset of C++ when writing the L compiler.</p><p><img src="'+a+'" alt="bootstrapping the L compiler"></p><h2 id="bootstrapping-the-ghul-compiler" tabindex="-1">bootstrapping the ghūl compiler <a class="header-anchor" href="#bootstrapping-the-ghul-compiler" aria-label="Permalink to &quot;bootstrapping the ghūl compiler&quot;">​</a></h2><p>The first version of the ghūl compiler was written in a language named L. L was a language I designed a</p><h3 id="l-to-l-transpiler-written-in-l" tabindex="-1">L to L transpiler written in L <a class="header-anchor" href="#l-to-l-transpiler-written-in-l" aria-label="Permalink to &quot;L to L transpiler written in L&quot;">​</a></h3><p>A few years later, when I decided to design the ghūl programming language, I needed a new compiler. I wrote this first version of the ghūl compiler in L. Initially, this compiler was a simple source-level L to L transpiler, and did not understand the ghūl language at all:</p><p><img src="'+i+'" alt="The L to L transpiling &#39;ghul compiler&#39;"></p><p>As this simple &#39;ghūl compiler&#39; performing no semantic analysis and generated L source code as its &#39;object code&#39;, it relied on the L compiler for semantic error detection and reporting and for x64 code generation.</p><p>The combination of this L to L transpiler plus the self hosting L compiler was an L compiler that produced x64 executables with an intermediate step of transpiled L as an intermediate language.</p><p><img src="'+o+'" alt="L compiler and the L to L transpiling &#39;ghul compiler&#39;"></p><p>Note we are running the transpiler and the compiler together in sequence as if the combination of the two was a single compiler. The input language of the transpiler is translated into the output language of the compiler. This is not yet bootstrapping, so we&#39;re not nesting the tombstone diagrams together as we would for a bootstrap.</p><p>This L-to-L transpiler was temporary scaffolding to support bootstrapping the ghūl compiler. It could in principle have been written any language with a compiler for the target architecture, but since I wrote the L compiler, choosing L meant I could make changes to the L language and compiler if needed to support the ghūl bootstrap.</p><p>The reason the initial transpiler translated L to L is that I needed it to be able to transpile itself, and to produce a language that a compiler already existed for. The plan being to use the transpiler to bootstrap itself from an existing language to the new language ghūl by re-writing its own source code in ghūl. I couldn&#39;t start with a transpiler written in ghūl, as no compiler would be able to compile it. Nor would there be any point in having the transpiler emit ghūl as again, no compiler would be able to compile this output. Once the transpiler was up and running, it could be modified to support ghūl input and output.</p><h3 id="ghul-to-l-transpiler-written-in-l" tabindex="-1">ghūl to L transpiler written in L <a class="header-anchor" href="#ghul-to-l-transpiler-written-in-l" aria-label="Permalink to &quot;ghūl to L transpiler written in L&quot;">​</a></h3><p>I then enhanced this L to L transpiler so it could transpile L to ghūl and ghūl to L. Both versions of this transpiler were still written in L and still relied on the L compiler as a backend to generate the x64 executable from transpiled the L code.</p><p><img src="'+r+'" alt="Transpilers handling both L and ghul"></p><h3 id="transpiler-bootstrap" tabindex="-1">transpiler bootstrap <a class="header-anchor" href="#transpiler-bootstrap" aria-label="Permalink to &quot;transpiler bootstrap&quot;">​</a></h3><p>I passed the L source code of the ghūl to L transpiler through the L to ghūl transpiler, generating output in ghūl. This yielded a ghūl to L transpiler written in ghūl</p><p><img src="'+n+'" alt="Bootstrapping the ghūl to L transpiler"></p><p><strong>This step is the first bootstrap</strong>: the source code of the ghūl to L transpiler that was L, has been transformed into ghūl, and so it can now transpile itself.</p><p>I enhanced the ghūl to L transpiler so it called the L compiler automatically, and this combination of ghūl to L transpiler followed by the self hosting L compiler became the self-hosting ghūl language compiler front end.</p><p><img src="'+c+'" alt="All transpilers"></p><p>However, this early version of the ghūl compiler was very crude and still depended on the L compiler for semantic analysis, error checking, and code generation.</p><h3 id="bootstrapping-from-l-backend-to-net-backend" tabindex="-1">bootstrapping from L backend to .NET backend <a class="header-anchor" href="#bootstrapping-from-l-backend-to-net-backend" aria-label="Permalink to &quot;bootstrapping from L backend to .NET backend&quot;">​</a></h3><p>Gradually, I integrated more semantic analysis into the ghūl compiler. This addition included representations of classes, traits, methods, functions, variables, etc., along with corresponding error checking and reporting.</p><p>With the compiler now capable of constructing a detailed representation of input programs, I added a .NET IL generation backend. I maintained the L source code backend alongside the .NET IL backend until the .NET IL backend was sufficiently complete and stable to self-host the compiler on .NET. This phase was awkward due to L&#39;s LLVM backend and its standard library, with features like generic collections and file handling, which interfaced with glibc and were not source compatible with similar facilities in .NET. I solved this by building a small subset of the .NET standard library in L, and then porting references to the L standard library in the ghūl compiler to use this .NET library subset, thus enabling the compiler to be built on and target both L and .NET.</p><p>Finally, with the compiler reliably self-hosting on .NET, I removed the L transpilation backend and the .NET library subset, and the compiler was successfully bootstrapped onto .NET.</p><p>You can see this process in the Git history in the ghūl compiler repo, going all the way back to the initial commit.</p>',43)])])}const T=l(g,[["render",d]]);export{y as __pageData,T as default};
