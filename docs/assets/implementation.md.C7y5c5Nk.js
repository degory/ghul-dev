import{_ as s,I as i,g as a,j as t}from"./chunks/framework.CbuNJfvM.js";const u=JSON.parse('{"title":"implementation","description":"","frontmatter":{},"headers":[],"relativePath":"implementation.md","filePath":"implementation.md"}'),n={name:"implementation.md"};function l(o,e,r,h,d,c){return i(),a("div",null,[...e[0]||(e[0]=[t('<h1 id="implementation" tabindex="-1">implementation <a class="header-anchor" href="#implementation" aria-label="Permalink to &quot;implementation&quot;">​</a></h1><p>The ghūl compiler is itself written in ghūl. It is published as the <a href="https://www.nuget.org/packages/ghul.compiler" target="_blank" rel="noreferrer"><code class="ghul-inline-plain">ghul.compiler</code></a> .NET tool, with source in the <a href="https://github.com/degory/ghul" target="_blank" rel="noreferrer"><code class="ghul-inline"><span style="--shiki-light:#001080;--shiki-dark:#9CDCFE;">degory</span><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">/</span><span style="--shiki-light:#001080;--shiki-dark:#9CDCFE;">ghul</span></code></a> repository.</p><h2 id="overall-shape" tabindex="-1">overall shape <a class="header-anchor" href="#overall-shape" aria-label="Permalink to &quot;overall shape&quot;">​</a></h2><p>A ghūl build moves through three broad stages:</p><ol><li><strong>Read the source.</strong> Source text is split into tokens, and the tokens are parsed into an AST: a tree of objects describing the program.</li><li><strong>Make sense of the source.</strong> A series of passes walks the AST, working out what every name refers to, what type every expression has, and reporting any errors.</li><li><strong>Emit IL.</strong> A final pass walks the AST and produces .NET intermediate language, which is assembled into a <code class="ghul-inline-plain">.dll</code> or <code class="ghul-inline-plain">.exe</code> on disk.</li></ol><p>When the compiler runs as the back end of the VS Code extension it follows the same first two stages but stops short of emitting IL, and runs many passes incrementally as the user edits.</p><h2 id="source-layout" tabindex="-1">source layout <a class="header-anchor" href="#source-layout" aria-label="Permalink to &quot;source layout&quot;">​</a></h2><p>The compiler&#39;s source is organised around the stages above. Each top-level folder under <code class="ghul-inline-plain">src/</code> in the compiler repository covers one concern:</p><table tabindex="0"><thead><tr><th>Folder</th><th>Role</th></tr></thead><tbody><tr><td><code class="ghul-inline-plain">lexical/</code></td><td>Turns source text into a stream of tokens.</td></tr><tr><td><code class="ghul-inline-plain">syntax/trees/</code></td><td>The AST node classes themselves.</td></tr><tr><td><code class="ghul-inline-plain">syntax/parsers/</code></td><td>Recursive-descent parsers that build the AST from tokens.</td></tr><tr><td><code class="ghul-inline-plain">syntax/process/</code></td><td>Passes that walk the AST: name resolution, type checking, IL generation, plus the editor-facing passes for completion and signature help.</td></tr><tr><td><code class="ghul-inline-plain">semantic/</code></td><td>The symbol table, scopes, types and supporting machinery used by the passes.</td></tr><tr><td><code class="ghul-inline-plain">ir/</code></td><td>A small set of nodes that model individual IL instructions, used while emitting code.</td></tr><tr><td><code class="ghul-inline-plain">compiler/</code></td><td>The <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">COMPILER</span></code> orchestrator that registers the passes and runs them over each source file.</td></tr><tr><td><code class="ghul-inline-plain">driver/</code></td><td>A thin command-line front-end that parses arguments and decides whether to run a build or the language service.</td></tr><tr><td><code class="ghul-inline-plain">analysis/</code></td><td>The language-service request handlers used in analysis mode.</td></tr><tr><td><code class="ghul-inline-plain">ioc/</code></td><td>A small inversion-of-control container. Mostly used by the parsers so they can refer to one another without circular constructor wiring.</td></tr><tr><td><code class="ghul-inline-plain">source/</code></td><td>Source-location bookkeeping. Every AST node has a <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">LOCATION</span></code>, so diagnostics, hovers and go-to-definition can point at the right span of text.</td></tr><tr><td><code class="ghul-inline-plain">logging/</code></td><td>Diagnostic reporting and per-pass timers. All compiler messages flow through <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">Logger</span></code> so that the IDE can intercept them.</td></tr></tbody></table><p>The boundaries are deliberately ordinary: anyone who has worked on a classical compiler will recognise most of them. The interesting parts are inside <code class="ghul-inline-plain">syntax/process/</code> and <code class="ghul-inline-plain">semantic/</code>.</p><h2 id="from-source-to-tokens" tabindex="-1">from source to tokens <a class="header-anchor" href="#from-source-to-tokens" aria-label="Permalink to &quot;from source to tokens&quot;">​</a></h2><p>The tokenizer in <code class="ghul-inline-plain">lexical/</code> reads source text a character at a time and produces a sequence of <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">TOKEN</span></code> values. Whitespace and comments are discarded; tokens know their kind (identifier, keyword, operator, string literal, …), their text and their source location. The tokenizer also handles a couple of features that are usually thought of as lexical: line continuation rules and the <code class="ghul-inline"><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">#</span><span style="--shiki-light:#AF00DB;--shiki-dark:#C586C0;">if</span></code> / <code class="ghul-inline"><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">#</span><span style="--shiki-light:#AF00DB;--shiki-dark:#C586C0;">else</span></code> preprocessor directives.</p><p>The parser in <code class="ghul-inline-plain">syntax/parsers/</code> is a hand-written recursive-descent parser. Each grammar production has its own parser class implementing a small <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">Parser</span><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">[</span><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">T</span><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">]</span></code> trait. Because the parsers refer to one another freely (an expression parser needs a statement parser, which in turn needs the expression parser), they are wired up through the IoC container so their references can be resolved lazily rather than via constructor arguments.</p><p>The output of the parser is an AST: a tree of <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">Node</span></code> subclasses defined in <code class="ghul-inline-plain">syntax/trees/</code>. The nodes fall into four broad groups:</p><ul><li><strong>Definitions</strong> - namespaces, classes, traits, structs, variants, functions, methods, fields, properties.</li><li><strong>Statements</strong> - <code class="ghul-inline"><span style="--shiki-light:#AF00DB;--shiki-dark:#C586C0;">if</span></code>, <code class="ghul-inline"><span style="--shiki-light:#AF00DB;--shiki-dark:#C586C0;">for</span></code>, <code class="ghul-inline"><span style="--shiki-light:#AF00DB;--shiki-dark:#C586C0;">while</span></code>, <code class="ghul-inline"><span style="--shiki-light:#AF00DB;--shiki-dark:#C586C0;">try</span></code>, and so on.</li><li><strong>Expressions</strong> - literals, calls, operators, lambdas, tuple constructors.</li><li><strong>Type expressions</strong> - types as they appear written in source, before the semantic layer turns them into the type objects described below.</li></ul><p>Every node knows the source location it came from, and the tree is the single shared data structure that the rest of the compiler walks.</p><h2 id="passes-over-the-ast" tabindex="-1">passes over the AST <a class="header-anchor" href="#passes-over-the-ast" aria-label="Permalink to &quot;passes over the AST&quot;">​</a></h2><p>The bulk of the compiler is a series of <em>passes</em> over the AST. Each pass is a class whose name describes what it does; most are subclasses of the <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">Visitor</span></code> hierarchy in <code class="ghul-inline-plain">syntax/process/</code>. The <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">COMPILER</span></code> class registers them in order, and each pass runs over every source file before the next pass starts. This means later passes can rely on the work the earlier passes have already done.</p><p>The order matters. For example, the pass that works out the type of an expression depends on the pass that has already resolved what every name refers to, which in turn depends on the pass that has put every declaration into the symbol table.</p><p>The full pass list, in the order <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">COMPILER</span></code> runs them:</p><table tabindex="0"><thead><tr><th>Pass</th><th>What it does</th></tr></thead><tbody><tr><td><code class="ghul-inline-plain">conditional-compilation</code></td><td>Removes any code disabled by <code class="ghul-inline"><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">#</span><span style="--shiki-light:#AF00DB;--shiki-dark:#C586C0;">if</span></code> directives.</td></tr><tr><td><code class="ghul-inline-plain">rewrite-syntax-trees</code></td><td>Light AST rewrites that simplify later passes: expanding nested namespaces into a flat list, generating accessor methods for properties, and spilling operand-position subexpressions sitting to the left of an <code class="ghul-inline"><span style="--shiki-light:#AF00DB;--shiki-dark:#C586C0;">await</span></code> so their values survive the suspend.</td></tr><tr><td><code class="ghul-inline-plain">declare-symbols</code></td><td>Walks the AST and registers every declaration (type, function, field, parameter, local) in the symbol table, attached to the appropriate scope.</td></tr><tr><td><code class="ghul-inline-plain">resolve-uses</code></td><td>Resolves identifier references to the declarations they name, using the scopes the previous pass set up.</td></tr><tr><td><code class="ghul-inline-plain">resolve-type-expressions</code></td><td>Turns type annotations as written in source into the semantic <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">Type</span></code> objects used by the rest of the compiler.</td></tr><tr><td><code class="ghul-inline-plain">resolve-ancestors</code></td><td>Attaches base classes and trait parents to the classes and traits that name them.</td></tr><tr><td><code class="ghul-inline-plain">resolve-explicit-types</code></td><td>Checks variables whose type is written out against the type of their initializer.</td></tr><tr><td><code class="ghul-inline-plain">resolve-overrides</code></td><td>Verifies that methods marked <code class="ghul-inline"><span style="--shiki-light:#001080;--shiki-dark:#9CDCFE;">override</span></code> match an inherited signature, and that overrides do not conflict.</td></tr><tr><td><code class="ghul-inline-plain">record-type-argument-uses</code></td><td>Notes which generic types are instantiated with which type arguments, so the IL pass can later produce the right open-generic references.</td></tr><tr><td><code class="ghul-inline-plain">mark-boxed-locals</code></td><td>Marks variables that have to live on the heap rather than in a local slot, typically because they are captured by a closure or by a generator&#39;s state machine.</td></tr><tr><td><code class="ghul-inline-plain">compile-expressions</code></td><td>The largest pass. Walks every expression in every function body, working out its type, resolving operator and method overloads, running type inference, narrowing, and producing IR values that describe what the IL should look like.</td></tr><tr><td><code class="ghul-inline-plain">warn-implicit-mutable-let</code></td><td>Emits warnings for <code class="ghul-inline"><span style="--shiki-light:#0000FF;--shiki-dark:#569CD6;">let</span></code> variables that are mutated but were not declared <code class="ghul-inline"><span style="--shiki-light:#0000FF;--shiki-dark:#569CD6;">mut</span></code>, when the corresponding compiler flag is set.</td></tr><tr><td><code class="ghul-inline-plain">generate-il</code></td><td>Walks the AST one last time and emits .NET IL, using the IR values produced by <code class="ghul-inline-plain">compile-expressions</code>.</td></tr></tbody></table><p>Whether each pass actually runs depends on the build flags. A plain syntax check stops after the early passes; a full build runs all of them. Analysis mode runs everything up to <code class="ghul-inline-plain">compile-expressions</code> but does not emit IL.</p><p>A short overview of each:</p><h3 id="conditional-compilation" tabindex="-1"><code class="ghul-inline-plain">conditional-compilation</code> <a class="header-anchor" href="#conditional-compilation" aria-label="Permalink to &quot;`conditional-compilation`{:text}&quot;">​</a></h3><p>ghūl has a <code class="ghul-inline"><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">#</span><span style="--shiki-light:#AF00DB;--shiki-dark:#C586C0;">if</span></code> / <code class="ghul-inline"><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">#</span><span style="--shiki-light:#AF00DB;--shiki-dark:#C586C0;">else</span></code> / <code class="ghul-inline"><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">#</span><span style="--shiki-light:#001080;--shiki-dark:#9CDCFE;">end</span></code> preprocessor for conditional compilation. This pass walks the AST and removes the branches that the current build flags exclude, so subsequent passes do not have to think about disabled code.</p><h3 id="rewrite-syntax-trees" tabindex="-1"><code class="ghul-inline-plain">rewrite-syntax-trees</code> <a class="header-anchor" href="#rewrite-syntax-trees" aria-label="Permalink to &quot;`rewrite-syntax-trees`{:text}&quot;">​</a></h3><p>A handful of AST rewrites that are easier to do up front than to handle everywhere afterwards. The notable ones are:</p><ul><li><strong>expand namespaces</strong> - flattens nested namespaces like <code class="ghul-inline"><span style="--shiki-light:#0000FF;--shiki-dark:#569CD6;">namespace</span><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;"> Foo</span><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">.</span><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">Bar</span><span style="--shiki-light:#0000FF;--shiki-dark:#569CD6;"> is</span><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;"> …</span></code> into the equivalent nested form <code class="ghul-inline"><span style="--shiki-light:#0000FF;--shiki-dark:#569CD6;">namespace</span><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;"> Foo</span><span style="--shiki-light:#0000FF;--shiki-dark:#569CD6;"> is</span><span style="--shiki-light:#0000FF;--shiki-dark:#569CD6;"> namespace</span><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;"> Bar</span><span style="--shiki-light:#0000FF;--shiki-dark:#569CD6;"> is</span><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;"> … </span><span style="--shiki-light:#0000FF;--shiki-dark:#569CD6;">si</span><span style="--shiki-light:#0000FF;--shiki-dark:#569CD6;"> si</span></code>.</li><li><strong>add accessors for properties</strong> - synthesises the underlying getter and setter methods that a property declaration stands for.</li><li><strong>spill awaits</strong> - at each composite expression containing an <code class="ghul-inline"><span style="--shiki-light:#AF00DB;--shiki-dark:#C586C0;">await</span></code>, wraps every earlier-evaluated sub-expression in a <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">SPILL</span></code> node. The async state machine described below suspends with an internal <code class="ghul-inline"><span style="--shiki-light:#001080;--shiki-dark:#9CDCFE;">leave</span></code> instruction, and the CLR requires its evaluation stack to be empty at the suspend point; anything spilled is stashed into a field on the state-machine frame so it survives the suspend.</li></ul><h3 id="declare-symbols" tabindex="-1"><code class="ghul-inline-plain">declare-symbols</code> <a class="header-anchor" href="#declare-symbols" aria-label="Permalink to &quot;`declare-symbols`{:text}&quot;">​</a></h3><p>Walks definitions in the AST and creates the symbol-table entries for them: types for each class, trait, struct and variant; functions and methods; fields, properties, parameters and local variables. Each declaration goes into the appropriate scope so that later passes can look it up.</p><p>This pass also scans every function body for <code class="ghul-inline"><span style="--shiki-light:#001080;--shiki-dark:#9CDCFE;">yield</span></code> and <code class="ghul-inline"><span style="--shiki-light:#AF00DB;--shiki-dark:#C586C0;">await</span></code> expressions and classifies the function accordingly: plain, generator (returns <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">Iterable</span><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">[</span><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">T</span><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">]</span></code> and contains <code class="ghul-inline"><span style="--shiki-light:#001080;--shiki-dark:#9CDCFE;">yield</span></code>), or asynchronous (contains <code class="ghul-inline"><span style="--shiki-light:#AF00DB;--shiki-dark:#C586C0;">await</span></code>). The classification is what later tells the IL pass to emit a generator or async state machine for the function instead of a straight method body.</p><h3 id="resolve-uses" tabindex="-1"><code class="ghul-inline-plain">resolve-uses</code> <a class="header-anchor" href="#resolve-uses" aria-label="Permalink to &quot;`resolve-uses`{:text}&quot;">​</a></h3><p>For every identifier used in the program, finds the declaration it refers to. Lexical scoping rules and namespace <code class="ghul-inline"><span style="--shiki-light:#0000FF;--shiki-dark:#569CD6;">use</span></code> statements are honoured here. Failed resolution becomes an <code class="ghul-inline"><span style="--shiki-light:#001080;--shiki-dark:#9CDCFE;">unknown</span><span style="--shiki-light:#001080;--shiki-dark:#9CDCFE;"> symbol</span></code> diagnostic.</p><h3 id="resolve-type-expressions" tabindex="-1"><code class="ghul-inline-plain">resolve-type-expressions</code> <a class="header-anchor" href="#resolve-type-expressions" aria-label="Permalink to &quot;`resolve-type-expressions`{:text}&quot;">​</a></h3><p>The shape of a type as written in source (<code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">List</span><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">[</span><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">Pair</span><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">[</span><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">int</span><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">, </span><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">string</span><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">]]</span></code>, <code class="ghul-inline"><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">(</span><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">int</span><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">, </span><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">string</span><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">) </span><span style="--shiki-light:#0000FF;--shiki-dark:#569CD6;">-&gt;</span><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;"> bool</span></code>, and so on) is parsed into AST nodes under <code class="ghul-inline-plain">type_expressions/</code>. This pass turns those into the <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">Type</span></code> objects (described under <a href="#types">types</a> below) that the rest of the compiler manipulates.</p><h3 id="resolve-ancestors" tabindex="-1"><code class="ghul-inline-plain">resolve-ancestors</code> <a class="header-anchor" href="#resolve-ancestors" aria-label="Permalink to &quot;`resolve-ancestors`{:text}&quot;">​</a></h3><p>Once class and trait names can be resolved to types, this pass attaches a class to its base class and the traits it implements, and a trait to its parent traits. The inheritance graph is then available for later passes to walk.</p><h3 id="resolve-explicit-types" tabindex="-1"><code class="ghul-inline-plain">resolve-explicit-types</code> <a class="header-anchor" href="#resolve-explicit-types" aria-label="Permalink to &quot;`resolve-explicit-types`{:text}&quot;">​</a></h3><p>For every variable whose type is written out, checks that the initializer&#39;s type is assignable to it. The check itself is mostly delegated to the type system; the pass exists to make the checks happen at a well-defined point.</p><h3 id="resolve-overrides" tabindex="-1"><code class="ghul-inline-plain">resolve-overrides</code> <a class="header-anchor" href="#resolve-overrides" aria-label="Permalink to &quot;`resolve-overrides`{:text}&quot;">​</a></h3><p>For every method marked <code class="ghul-inline"><span style="--shiki-light:#001080;--shiki-dark:#9CDCFE;">override</span></code>, checks that an ancestor defines a matching virtual or abstract method, and that the override is consistent with it. Reports duplicate global functions where two top-level functions cannot be told apart by their signatures.</p><h3 id="record-type-argument-uses" tabindex="-1"><code class="ghul-inline-plain">record-type-argument-uses</code> <a class="header-anchor" href="#record-type-argument-uses" aria-label="Permalink to &quot;`record-type-argument-uses`{:text}&quot;">​</a></h3><p>Collects the set of concrete generic-argument combinations the program actually uses, so that the IL pass can emit the right references. The ghūl IL emitter prefers open-generic references (<code class="ghul-inline"><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">!</span><span style="--shiki-light:#098658;--shiki-dark:#B5CEA8;">0</span></code> for the first class-level type parameter, <code class="ghul-inline"><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">!!</span><span style="--shiki-light:#098658;--shiki-dark:#B5CEA8;">0</span></code> for the first method-level one) rather than substituting the concrete type into the signature itself; this pass collects the information that lets it do so.</p><h3 id="mark-boxed-locals" tabindex="-1"><code class="ghul-inline-plain">mark-boxed-locals</code> <a class="header-anchor" href="#mark-boxed-locals" aria-label="Permalink to &quot;`mark-boxed-locals`{:text}&quot;">​</a></h3><p>Some variables cannot live in a local slot. The clearest case is a local captured by a lambda: the lambda outlives the call that created it, so the local has to move onto the heap. Generator and asynchronous functions are a similar case: both compile to state-machine classes whose locals become fields, so that the function&#39;s state survives a <code class="ghul-inline"><span style="--shiki-light:#001080;--shiki-dark:#9CDCFE;">yield</span></code> or <code class="ghul-inline"><span style="--shiki-light:#AF00DB;--shiki-dark:#C586C0;">await</span></code>. This pass walks the AST and marks each affected variable so the IL pass emits the right shape of code for it.</p><h3 id="compile-expressions" tabindex="-1"><code class="ghul-inline-plain">compile-expressions</code> <a class="header-anchor" href="#compile-expressions" aria-label="Permalink to &quot;`compile-expressions`{:text}&quot;">​</a></h3><p>The most substantial pass. It walks every expression in every function body and:</p><ul><li>works out the type of every expression and sub-expression;</li><li>resolves operator and method overloads;</li><li>runs type inference for unannotated locals, lambda parameters, list literal element types, generic type arguments at call sites, and generic constructors;</li><li>applies type narrowing through <code class="ghul-inline"><span style="--shiki-light:#0000FF;--shiki-dark:#569CD6;">isa</span></code> checks, <code class="ghul-inline"><span style="--shiki-light:#AF00DB;--shiki-dark:#C586C0;">if</span><span style="--shiki-light:#0000FF;--shiki-dark:#569CD6;"> let</span></code>, null tests and variant tests;</li><li>produces <em>IR values</em> that describe, for each expression, the sequence of IL operations it stands for.</li></ul><p>Inference inside this pass is bidirectional: information flows up from inner expressions, and back down from contexts that constrain what an expression&#39;s type can be. A function&#39;s signature is always explicit, so inference is confined to function bodies and never changes anything visible from outside the function.</p><h3 id="warn-implicit-mutable-let" tabindex="-1"><code class="ghul-inline-plain">warn-implicit-mutable-let</code> <a class="header-anchor" href="#warn-implicit-mutable-let" aria-label="Permalink to &quot;`warn-implicit-mutable-let`{:text}&quot;">​</a></h3><p>A <code class="ghul-inline"><span style="--shiki-light:#0000FF;--shiki-dark:#569CD6;">let</span></code> that is assigned to after its initializer counts as a mutable variable but does not say so on its declaration. When the corresponding warning is enabled, this pass walks the AST and reports each one. It is structurally independent of <code class="ghul-inline-plain">compile-expressions</code> but runs after it because it needs the assignment information that pass has already collected.</p><h3 id="generate-il" tabindex="-1"><code class="ghul-inline-plain">generate-il</code> <a class="header-anchor" href="#generate-il" aria-label="Permalink to &quot;`generate-il`{:text}&quot;">​</a></h3><p>The final pass writes the IR values produced by <code class="ghul-inline-plain">compile-expressions</code> out as .NET IL. The compiler does not link to a managed-code generator; it writes textual IL to a file and then invokes the <code class="ghul-inline-plain">ilasm</code> tool from the .NET SDK to assemble it into a <code class="ghul-inline-plain">.dll</code> or <code class="ghul-inline-plain">.exe</code>.</p><h2 id="the-main-data-structures" tabindex="-1">the main data structures <a class="header-anchor" href="#the-main-data-structures" aria-label="Permalink to &quot;the main data structures&quot;">​</a></h2><p>A handful of data structures are visible across most of the passes.</p><h3 id="the-ast" tabindex="-1">the AST <a class="header-anchor" href="#the-ast" aria-label="Permalink to &quot;the AST&quot;">​</a></h3><p>The output of the parser and the working medium of every subsequent pass. Each <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">Node</span></code> subclass models one piece of syntax (a class definition, an <code class="ghul-inline"><span style="--shiki-light:#AF00DB;--shiki-dark:#C586C0;">if</span></code> statement, a method call) and knows the source location it came from. As later passes work, they attach additional information to the nodes: an identifier use gets a reference to the symbol it refers to, an expression gets its inferred type, and the IL pass attaches IR values that record how to emit it.</p><h3 id="symbols" tabindex="-1">symbols <a class="header-anchor" href="#symbols" aria-label="Permalink to &quot;symbols&quot;">​</a></h3><p>A <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">SYMBOL</span></code> is the compiler&#39;s record of one thing declared in the program: a type, a function, a method, a field, a local variable, a parameter, a generic type argument, and so on. There is one subclass per flavour (<code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">CLASS</span></code>, <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">INSTANCE_METHOD</span></code>, <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">VARIABLE</span></code> and the rest), and they all live under <code class="ghul-inline-plain">semantic/symbols/</code>.</p><p>The <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">SYMBOL_TABLE</span></code> is a central registry; the <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">NAMESPACES</span></code> object tracks namespace membership. Together they answer the question &quot;what does this name refer to?&quot;.</p><h3 id="scopes" tabindex="-1">scopes <a class="header-anchor" href="#scopes" aria-label="Permalink to &quot;scopes&quot;">​</a></h3><p>A scope is the answer to &quot;what names are visible here?&quot;. Scopes nest: the innermost is typically a block scope, inside a method scope, inside a class scope, inside a namespace scope. Name resolution walks outwards through the chain of scopes until it finds a match.</p><p>The base <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">Scope</span></code> and its specialisations (<code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">BLOCK_SCOPE</span></code>, <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">NAMESPACE_SCOPE</span></code>) live under <code class="ghul-inline-plain">semantic/scope/</code>.</p><h3 id="types" tabindex="-1">types <a class="header-anchor" href="#types" aria-label="Permalink to &quot;types&quot;">​</a></h3><p>A <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">Type</span></code> represents a ghūl type in the form the compiler uses internally. The hierarchy under <code class="ghul-inline-plain">semantic/types/</code> is fairly small:</p><ul><li><code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">NAMED</span></code> - a reference to a named type (class, trait, struct, variant, primitive).</li><li><code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">GENERIC</span></code> - a generic type applied to type arguments, such as <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">List</span><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">[</span><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">int</span><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">]</span></code>.</li><li><code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">FUNCTION</span></code> - a function type, used for first-class functions, lambdas, and methods.</li><li><code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">TUPLE</span></code> - a tuple type, with optional element names.</li><li><code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">ARRAY</span></code> - a fixed array type.</li><li><code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">OPTION</span></code>, <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">ONE_OF</span></code>, <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">NONE</span></code>, <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">NULL</span></code> - optional and union variations.</li><li><code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">INFERRED_VARIABLE_TYPE</span></code> and friends - placeholders used during inference, replaced by concrete types as constraints accumulate.</li><li><code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">ERROR</span></code> - stands in for the type of an expression the compiler could not work out, so that later passes can continue without cascading every diagnostic.</li></ul><p><code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">Type</span></code> objects answer most of the questions the compiler has about expressions: is this assignable to that, what is the common type of these branches, what overload best matches these argument types.</p><h3 id="ir-values" tabindex="-1">IR values <a class="header-anchor" href="#ir-values" aria-label="Permalink to &quot;IR values&quot;">​</a></h3><p><code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">IR</span><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">.</span><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">Value</span></code> subclasses model individual operations to be emitted as IL. Most map one-for-one to a single IL instruction; a few (<code class="ghul-inline"><span style="--shiki-light:#001080;--shiki-dark:#9CDCFE;">tuple</span></code>, <code class="ghul-inline"><span style="--shiki-light:#0000FF;--shiki-dark:#569CD6;">isa</span></code>, generic boxing helpers) expand into a small sequence. <code class="ghul-inline-plain">compile-expressions</code> builds an IR value tree for each expression; <code class="ghul-inline-plain">generate-il</code> walks those trees and prints IL.</p><h3 id="diagnostics" tabindex="-1">diagnostics <a class="header-anchor" href="#diagnostics" aria-label="Permalink to &quot;diagnostics&quot;">​</a></h3><p>Errors, warnings and informational messages all flow through <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">Logger</span></code> in <code class="ghul-inline-plain">logging/</code>, which attaches a <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">Source</span><span style="--shiki-light:#000000;--shiki-dark:#D4D4D4;">.</span><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">LOCATION</span></code> to each message and stores them in <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">DIAGNOSTICS_STORE</span></code>. The IDE retrieves diagnostics by reading the store after a compile.</p><h2 id="analysis-mode" tabindex="-1">analysis mode <a class="header-anchor" href="#analysis-mode" aria-label="Permalink to &quot;analysis mode&quot;">​</a></h2><p>The same compiler executable runs the IDE&#39;s language service. When the ghūl VS Code extension starts, it launches <code class="ghul-inline-plain">ghul-compiler</code> with the <code class="ghul-inline-plain">--analyse</code> flag, then talks to it over the process&#39;s standard input and output streams.</p><p>Communication uses a small text protocol. The IDE sends a request as a keyword like <code class="ghul-inline-plain">#EDIT#</code>, <code class="ghul-inline-plain">#COMPILE#</code>, <code class="ghul-inline-plain">#HOVER#</code>, <code class="ghul-inline-plain">#DEFINITION#</code>, <code class="ghul-inline-plain">#COMPLETE#</code> or <code class="ghul-inline-plain">#HOVERMAP#</code>, followed by any arguments it needs on subsequent lines. The compiler replies with a header line, optional result lines, and ends each response with a form-feed character so the IDE knows the reply is complete. The protocol was chosen to be simple enough to implement without a JSON parser; the format is stable and the editor is expected to ignore lines it does not recognise.</p><p>The interesting work happens around two requests:</p><ul><li><strong><code class="ghul-inline-plain">#EDIT#</code></strong> - sent on every keystroke (after a short debounce). Re-parses the file the user is editing and re-runs the early passes over it, keeping the rest of the project&#39;s AST untouched. This is fast enough to keep up with typing, and it is what produces the squiggles and hovers that appear as the user types.</li><li><strong><code class="ghul-inline-plain">#COMPILE#</code></strong> - sent during a longer pause in typing. Runs the full pass sequence over the whole project so that any consequences of the edit ripple through the rest of the analysis.</li></ul><p>This two-stage pattern keeps the typical-case latency low without sacrificing correctness once the user pauses. Hover information, go-to-definition, completions and signature help all come from the state these passes maintain: the symbol table, the scopes, the per-AST-node type annotations and the symbol-use map.</p><p>Two convenience requests, <code class="ghul-inline-plain">#HOVERMAP#</code> and <code class="ghul-inline-plain">#SEMANTICTOKENS#</code>, dump every recorded hover or every recorded symbol use in one batch. They are mainly used by the example pipeline on this website, which feeds each example through the analyser and uses the results to drive hover popups and semantic-token colouring in the rendered output.</p><p>The compiler runs as a long-lived process. A <code class="ghul-inline"><span style="--shiki-light:#267F99;--shiki-dark:#4EC9B0;">WATCHDOG</span></code> component watches for sustained error bursts or excessive heap growth and asks the IDE to recycle the process when either threshold is crossed; recycles happen fairly often, but the extension schedules them during idle periods so the user rarely notices. Even with the recycles, the long-lived shape is much cheaper than starting a fresh compiler for every request.</p><h2 id="bootstrap-and-self-hosting" tabindex="-1">bootstrap and self-hosting <a class="header-anchor" href="#bootstrap-and-self-hosting" aria-label="Permalink to &quot;bootstrap and self-hosting&quot;">​</a></h2><p>The ghūl compiler is <em>self-hosting</em>: it is written in ghūl and is compiled by an earlier version of itself. Every CI build re-bootstraps the compiler by compiling the source under the published version and then comparing the IL produced by two further passes; the compiler that the publish produced should compile itself byte-for-byte identically. A divergence between the two is treated as a build failure.</p><p>The historical story of how the first version of the compiler came to exist, back when no ghūl compiler existed to compile it, is on the <a href="/history">history</a> page.</p>',82)])])}const g=s(n,[["render",l]]);export{u as __pageData,g as default};
