import{_ as t,a,b as i,c as o,d as r,e as n}from"./chunks/bootstrap-ghūl-self-hosting-transpiler.drawio.WkZHwCzJ.js";import{_ as s,I as l,g as h,j as p}from"./chunks/framework.CbuNJfvM.js";const y=JSON.parse('{"title":"history","description":"","frontmatter":{},"headers":[],"relativePath":"history.md","filePath":"history.md"}'),c={name:"history.md"};function d(g,e,m,u,b,f){return l(),h("div",null,[...e[0]||(e[0]=[p('<h1 id="history" tabindex="-1">history <a class="header-anchor" href="#history" aria-label="Permalink to &quot;history&quot;">​</a></h1><h2 id="background" tabindex="-1">background <a class="header-anchor" href="#background" aria-label="Permalink to &quot;background&quot;">​</a></h2><p>The ghūl <a href="https://github.com/degory/ghul" target="_blank" rel="noreferrer">compiler</a> is a <a href="https://en.wikipedia.org/wiki/Self-hosting_(compilers)" target="_blank" rel="noreferrer">self-hosting compiler</a>, capable of compiling itself from its own source code.</p><p><a href="https://en.wikipedia.org/wiki/Bootstrapping_(compilers)" target="_blank" rel="noreferrer">Bootstrapping</a> a new compiler for a new language is a classic chicken-and-egg problem: you need a compiler to compile your new compiler, but that compiler doesn’t exist yet.</p><p>The solution is to write the initial compiler in a language that does exist; then, once that compiler is sufficiently reliable, its source code must be translated into the new language, giving a compiler that can compile the new language, including its own source code. It&#39;s best to keep the initial compiler simple, for example by handling only a subset of the new language and producing unoptimized code. The smaller and simpler the initial compiler is, the less work is involved in translating it into the new language, making bootstrapping easier.</p><h2 id="tombstones" tabindex="-1">tombstones <a class="header-anchor" href="#tombstones" aria-label="Permalink to &quot;tombstones&quot;">​</a></h2><p>When discussing bootstrapping, it&#39;s common to use <a href="https://en.wikipedia.org/wiki/Tombstone_diagram" target="_blank" rel="noreferrer">tombstone</a> diagrams. In a T shaped tombstone diagram, the T represents a particular version of a compiler. Within the T, three languages are named: the source language, the target language, and the implementation language. The target language is typically some kind of IL or machine language, but for a transpiler, the target language is source code.</p><p>For example, a compiler C that reads source in language S, translates it to language T, and is implemented in language I, would be depicted like this:</p><p><img src="'+t+'" alt="Example tombstone showing compiler for language S, targeting language T, written in language I"></p><h2 id="the-l-compiler" tabindex="-1">the L compiler <a class="header-anchor" href="#the-l-compiler" aria-label="Permalink to &quot;the L compiler&quot;">​</a></h2><p>ghūl grew out of another language I designed, years before, named L, and I used L in the early stages of the ghūl compiler development. The initial ghūl compiler could in principle have been written in any pre-existing language with a compiler; I chose L because I knew I could easily make changes to the L language and compiler if needed to support the ghūl bootstrap.</p><p>(Plus, why spend ages designing a programming language and building a compiler for it, if you&#39;re not going to use it to build <em>another</em> compiler for a <em>another</em> programming language you&#39;ve invented? 😂)</p><p>Like the ghūl compiler, the L compiler is self-hosting. It was originally written in C++ and I bootstrapped it manually by hand translating the C++ source into equivalent L. This tedious process was made easier by carefully sticking to an L-compatible subset of C++ when writing the initial C++ version of the L compiler.</p><p><img src="'+a+'" alt="bootstrapping the L compiler"></p><h2 id="l-to-l-transpiler" tabindex="-1">L to L transpiler <a class="header-anchor" href="#l-to-l-transpiler" aria-label="Permalink to &quot;L to L transpiler&quot;">​</a></h2><p>The first version of the ghūl compiler wasn&#39;t really a compiler and it didn&#39;t understand ghūl. It was actually a simple L source code to L source code transpiler, written in L.</p><p><img src="'+i+'" alt="The L to L transpiling &#39;ghul compiler&#39;"></p><p>This transpiler was in part a strategic piece of scaffolding, intended to support the bootstrap process. I wanted to automate the process of transforming the compiler source code from L to ghūl and so avoid the tedious hand-translation I&#39;d had to do previously when bootstrapping the L compiler. Some parts of the L to L transpiler do survive in the real ghūl compiler (the lexical analyzer and the parser), but the rest was discarded once the initial bootstrap was complete</p><p>I added a compiler driver to the L to L transpiler, so it could call the existing L compiler, passing in the L code it generated. The result was a compiler that consumed L source code and generated x64 machine code, but which also generated transpiled L source code as an intermediate step.</p><p><img src="'+o+'" alt="L compiler and the L to L transpiling &#39;ghul compiler&#39;"></p><p>This simple &#39;ghūl compiler&#39; still understood no ghūl, performed no semantic analysis, and generated L source code as its &#39;object code&#39;. It relied completely on the L compiler for semantic error detection and reporting and for x64 code generation.</p><h2 id="l-to-ghul-and-ghul-to-l-transpilers" tabindex="-1">L to ghūl and ghūl to L transpilers <a class="header-anchor" href="#l-to-ghul-and-ghul-to-l-transpilers" aria-label="Permalink to &quot;L to ghūl and ghūl to L transpilers&quot;">​</a></h2><p>I enhanced the L to L transpiler so it could transpile L to ghūl and ghūl to L. The transpiler was still written in L and still relied on the L compiler as a backend to generate the x64 executable from transpiled the L code.</p><p><img src="'+r+'" alt="Transpilers handling both L and ghul"></p><h2 id="first-ghul-bootstrap" tabindex="-1">first ghūl bootstrap <a class="header-anchor" href="#first-ghul-bootstrap" aria-label="Permalink to &quot;first ghūl bootstrap&quot;">​</a></h2><p>Then I passed the L source code of the ghūl to L transpiler through the L to ghūl transpiler, generating output in ghūl. This yielded a ghūl to L transpiler written in ghūl:</p><p><img src="'+n+'" alt="Bootstrapping the ghūl to L transpiler"></p><p><strong>This step is the first bootstrap</strong>, and it&#39;s why I started with a transpiler: the source code of the ghūl to L transpiler, which was originally L, has now been mechanically transformed into ghūl. This new transpiler is written in ghūl, and it can transpile itself into L: it has become self-hosting, albeit still dependent on the L compiler to generate machine code from L.</p><h2 id="net-backend" tabindex="-1">.NET backend <a class="header-anchor" href="#net-backend" aria-label="Permalink to &quot;.NET backend&quot;">​</a></h2><p>Gradually, I integrated more semantic analysis into the ghūl compiler: representations of classes, traits, methods, functions, variables, etc., along with corresponding error checking and reporting.</p><p>With the compiler now capable of constructing a detailed representation of input programs, I began implementing a .NET IL generation backend. I did this in stages, adding support for expressions and local variable definitions first, then working through the other more advanced language constructs.</p><p>I initially used scaffolding to test generated IL snippets, because the compiler wasn&#39;t capable of generating completely self-contained IL programs. As I implemented .NET IL for the various ghūl language features, and the compiler became increasingly more capable, I guarded against regressions by building <a href="https://github.com/degory/ghul/tree/main/integration-tests" target="_blank" rel="noreferrer">integration tests</a> as I went. The regression test runner started out as a collection of bash scripts, but as the compiler stabilized, I <a href="https://github.com/degory/ghul-test" target="_blank" rel="noreferrer">rewrote it in ghūl</a>. The initial regression suite included tests asserting correct IL generation for all the different ghūl language constructs as I implemented them. As soon as the compiler could generate code for complete programs, I added further integration tests exercising generated code execution.</p><p>I continued to maintain the transpilation to L source code backend alongside the .NET IL backend throughout this process, until the .NET IL backend was sufficiently complete and stable to self-host the compiler on .NET.</p><p>This phase was complicated by L&#39;s LLVM backend and by its standard library, with features like generic collections and file handling, which interfaced with glibc and were not source compatible with similar facilities in .NET. I needed to be able to use these facilities in the compiler source, and have the built compiler run on both glibc and .NET. I solved this by cloning a small subset of the .NET standard library in ghūl, and then porting references to the L standard library in the ghūl compiler to use this .NET library subset, thus enabling the compiler to be built on and target both L and .NET.</p><h2 id="full-self-hosting-on-net" tabindex="-1">full self-hosting on .NET <a class="header-anchor" href="#full-self-hosting-on-net" aria-label="Permalink to &quot;full self-hosting on .NET&quot;">​</a></h2><p>Finally, with the compiler reliably self-hosting on .NET, I removed the L transpilation backend and the .NET library subset, and the compiler was successfully bootstrapped onto .NET.</p><p>You can see this process in the Git history in the <a href="https://github.com/degory/ghul" target="_blank" rel="noreferrer">ghūl compiler repo</a>, going all the way back to the initial commit.</p>',37)])])}const k=s(c,[["render",d]]);export{y as __pageData,k as default};
