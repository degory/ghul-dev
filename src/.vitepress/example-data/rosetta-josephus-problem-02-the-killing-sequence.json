{
  "name": "rosetta-josephus-problem-02-the-killing-sequence",
  "code": "use default\n\nkillings(n: int, k: int) -> Pipe[int] is\n    let standing = (0..n) |> collect_list()\n    let at mut = 0\n\n    while standing.count > 1 do\n        at = (at + k - 1) % standing.count\n\n        yield standing[at]\n\n        standing.remove_at(at)\n    od\nsi\n\nshow(n: int, k: int) is\n    let killed = killings(n, k) |> collect_list()\n\n    write_line(\"n = {n}, k = {k}: killed {killed |> join(\", \")}\")\nsi\n\nshow(5, 2)\nshow(7, 3)",
  "fullSource": "use default\n\nkillings(n: int, k: int) -> Pipe[int] is\n    let standing = (0..n) |> collect_list()\n    let at mut = 0\n\n    while standing.count > 1 do\n        at = (at + k - 1) % standing.count\n\n        yield standing[at]\n\n        standing.remove_at(at)\n    od\nsi\n\nshow(n: int, k: int) is\n    let killed = killings(n, k) |> collect_list()\n\n    write_line(\"n = {n}, k = {k}: killed {killed |> join(\", \")}\")\nsi\n\nshow(5, 2)\nshow(7, 3)",
  "output": "n = 5, k = 2: killed 1, 3, 0, 4\nn = 7, k = 3: killed 2, 5, 1, 6, 4, 0\n",
  "images": [],
  "playground": true,
  "playgroundPath": "josephus-problem/02-the-killing-sequence",
  "hovers": [],
  "diagnostics": []
}
