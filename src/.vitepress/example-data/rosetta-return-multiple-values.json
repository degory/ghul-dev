{
  "name": "rosetta-return-multiple-values",
  "code": "use IO.Std.write_line\n\n// A tuple is the return type, so the elements can be named and the call\n// site can take them apart.\ndivide(dividend: int, divisor: int) -> (quotient: int, remainder: int) =>\n    (quotient = dividend / divisor, remainder = dividend % divisor)\n\nlet (quotient, remainder) = divide(17, 5)\n\nwrite_line(\"17 / 5 is {quotient} remainder {remainder}\")\n\nlet result = divide(23, 4)\n\nwrite_line(\"23 / 4 is {result.quotient} remainder {result.remainder}\")",
  "fullSource": "use IO.Std.write_line\n\n// A tuple is the return type, so the elements can be named and the call\n// site can take them apart.\ndivide(dividend: int, divisor: int) -> (quotient: int, remainder: int) =>\n    (quotient = dividend / divisor, remainder = dividend % divisor)\n\nlet (quotient, remainder) = divide(17, 5)\n\nwrite_line(\"17 / 5 is {quotient} remainder {remainder}\")\n\nlet result = divide(23, 4)\n\nwrite_line(\"23 / 4 is {result.quotient} remainder {result.remainder}\")",
  "output": "17 / 5 is 3 remainder 2\n23 / 4 is 5 remainder 3\n",
  "images": [],
  "playground": true,
  "playgroundPath": "return-multiple-values",
  "hovers": [],
  "diagnostics": []
}
