{
  "name": "rosetta-sorting-algorithms-merge-sort",
  "code": "use default\n\nmerge_sort[T: Ghul.Comparable[T]](values: List[T]) -> List[T] is\n    if values.count <= 1 then\n        return values\n    fi\n\n    let middle = values.count / 2\n\n    return merge(\n        merge_sort(values[0..middle]),\n        merge_sort(values[middle..<0]))\nsi\n\nmerge[T: Ghul.Comparable[T]](left: List[T], right: List[T]) -> List[T] is\n    let merged = LIST()\n    let l mut = 0\n    let r mut = 0\n\n    while l < left.count /\\ r < right.count do\n        if left[l] <= right[r] then\n            merged.add(left[l])\n            l = l + 1\n        else\n            merged.add(right[r])\n            r = r + 1\n        fi\n    od\n\n    while l < left.count do\n        merged.add(left[l])\n        l = l + 1\n    od\n\n    while r < right.count do\n        merged.add(right[r])\n        r = r + 1\n    od\n\n    return merged\nsi\n\nwrite_line(merge_sort([5, 3, 8, 1, 9, 2, 7]) |> join(\", \"))\nwrite_line(merge_sort([\"pear\", \"apple\", \"quince\", \"banana\"]) |> join(\", \"))",
  "fullSource": "use default\n\nmerge_sort[T: Ghul.Comparable[T]](values: List[T]) -> List[T] is\n    if values.count <= 1 then\n        return values\n    fi\n\n    let middle = values.count / 2\n\n    return merge(\n        merge_sort(values[0..middle]),\n        merge_sort(values[middle..<0]))\nsi\n\nmerge[T: Ghul.Comparable[T]](left: List[T], right: List[T]) -> List[T] is\n    let merged = LIST()\n    let l mut = 0\n    let r mut = 0\n\n    while l < left.count /\\ r < right.count do\n        if left[l] <= right[r] then\n            merged.add(left[l])\n            l = l + 1\n        else\n            merged.add(right[r])\n            r = r + 1\n        fi\n    od\n\n    while l < left.count do\n        merged.add(left[l])\n        l = l + 1\n    od\n\n    while r < right.count do\n        merged.add(right[r])\n        r = r + 1\n    od\n\n    return merged\nsi\n\nwrite_line(merge_sort([5, 3, 8, 1, 9, 2, 7]) |> join(\", \"))\nwrite_line(merge_sort([\"pear\", \"apple\", \"quince\", \"banana\"]) |> join(\", \"))",
  "output": "1, 2, 3, 5, 7, 8, 9\napple, banana, pear, quince\n",
  "images": [],
  "playground": true,
  "playgroundPath": "sorting-algorithms-merge-sort",
  "hovers": [],
  "diagnostics": []
}
