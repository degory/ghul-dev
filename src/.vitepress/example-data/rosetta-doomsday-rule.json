{
  "name": "rosetta-doomsday-rule",
  "code": "use IO.Std.write_line\n\nlet day_names = [\n    \"Sunday\", \"Monday\", \"Tuesday\", \"Wednesday\",\n    \"Thursday\", \"Friday\", \"Saturday\"\n]\n\nlet month_names = [\n    \"January\", \"February\", \"March\", \"April\", \"May\", \"June\",\n    \"July\", \"August\", \"September\", \"October\", \"November\", \"December\"\n]\n\nis_leap_year(year: int) -> bool =>\n    year % 4 == 0 /\\ (year % 100 != 0 \\/ year % 400 == 0)\n\nanchor_day(year: int, month: int) -> int =>\n    case month\n    when 1 then if is_leap_year(year) then 4 else 3 fi\n    when 2 then if is_leap_year(year) then 29 else 28 fi\n    when 3 then 7\n    when 4 then 4\n    when 5 then 9\n    when 6 then 6\n    when 7 then 11\n    when 8 then 8\n    when 9 then 5\n    when 10 then 10\n    when 11 then 7\n    else 12\n    esac\n\ndoomsday(year: int) -> int =>\n    (2 + 5 * (year % 4) + 4 * (year % 100) + 6 * (year % 400)) % 7\n\nday_of_week(year: int, month: int, day: int) -> int =>\n    (((doomsday(year) + day - anchor_day(year, month)) % 7) + 7) % 7\n\nlet dates = [\n    (year = 1800, month = 1, day = 6),\n    (year = 1875, month = 3, day = 29),\n    (year = 1915, month = 12, day = 7),\n    (year = 1970, month = 12, day = 23),\n    (year = 2043, month = 5, day = 14),\n    (year = 2077, month = 2, day = 12),\n    (year = 2101, month = 4, day = 2)\n]\n\nfor date in dates do\n    let name = day_names[day_of_week(date.year, date.month, date.day)]\n    let month = month_names[date.month - 1]\n\n    write_line(\"{month} {date.day}, {date.year} is a {name}\")\nod",
  "fullSource": "use IO.Std.write_line\n\nlet day_names = [\n    \"Sunday\", \"Monday\", \"Tuesday\", \"Wednesday\",\n    \"Thursday\", \"Friday\", \"Saturday\"\n]\n\nlet month_names = [\n    \"January\", \"February\", \"March\", \"April\", \"May\", \"June\",\n    \"July\", \"August\", \"September\", \"October\", \"November\", \"December\"\n]\n\nis_leap_year(year: int) -> bool =>\n    year % 4 == 0 /\\ (year % 100 != 0 \\/ year % 400 == 0)\n\nanchor_day(year: int, month: int) -> int =>\n    case month\n    when 1 then if is_leap_year(year) then 4 else 3 fi\n    when 2 then if is_leap_year(year) then 29 else 28 fi\n    when 3 then 7\n    when 4 then 4\n    when 5 then 9\n    when 6 then 6\n    when 7 then 11\n    when 8 then 8\n    when 9 then 5\n    when 10 then 10\n    when 11 then 7\n    else 12\n    esac\n\ndoomsday(year: int) -> int =>\n    (2 + 5 * (year % 4) + 4 * (year % 100) + 6 * (year % 400)) % 7\n\nday_of_week(year: int, month: int, day: int) -> int =>\n    (((doomsday(year) + day - anchor_day(year, month)) % 7) + 7) % 7\n\nlet dates = [\n    (year = 1800, month = 1, day = 6),\n    (year = 1875, month = 3, day = 29),\n    (year = 1915, month = 12, day = 7),\n    (year = 1970, month = 12, day = 23),\n    (year = 2043, month = 5, day = 14),\n    (year = 2077, month = 2, day = 12),\n    (year = 2101, month = 4, day = 2)\n]\n\nfor date in dates do\n    let name = day_names[day_of_week(date.year, date.month, date.day)]\n    let month = month_names[date.month - 1]\n\n    write_line(\"{month} {date.day}, {date.year} is a {name}\")\nod",
  "output": "January 6, 1800 is a Monday\nMarch 29, 1875 is a Monday\nDecember 7, 1915 is a Tuesday\nDecember 23, 1970 is a Wednesday\nMay 14, 2043 is a Thursday\nFebruary 12, 2077 is a Friday\nApril 2, 2101 is a Saturday\n",
  "images": [],
  "playground": true,
  "playgroundPath": "doomsday-rule",
  "hovers": [],
  "diagnostics": []
}
