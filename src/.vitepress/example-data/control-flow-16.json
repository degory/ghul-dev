{
  "name": "control-flow-16",
  "code": "if let (name, _) = lookup(id) then\n    write_line(\"found {name}\");\nfi",
  "fullSource": "if let (name, _) = lookup(id) then\n    write_line(\"found {name}\");\nfi\n",
  "output": "",
  "diagnostics": [],
  "hovers": []
}
