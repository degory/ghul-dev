{
  "name": "rosetta-ordered-partitions",
  "code": "use default\n\ncombinations(items: List[int], k: int) -> Pipe[List[int]] is\n    if k == 0 then\n        yield LIST()\n    elif items.count >= k then\n        let rest = items[1..<0]\n\n        for tail in combinations(rest, k - 1) do\n            let whole = LIST()\n\n            whole.add(items[0])\n\n            for value in tail do\n                whole.add(value)\n            od\n\n            yield whole\n        od\n\n        yield in combinations(rest, k)\n    fi\nsi\n\npartitions(items: List[int], sizes: List[int]) -> Pipe[List[List[int]]] is\n    if sizes.count == 0 then\n        yield LIST()\n    else\n        for block in combinations(items, sizes[0]) do\n            let remaining = items |> except(block) |> collect()\n\n            for tail in partitions(remaining, sizes[1..<0]) do\n                let whole = LIST()\n\n                whole.add(block)\n\n                for rest in tail do\n                    whole.add(rest)\n                od\n\n                yield whole\n            od\n        od\n    fi\nsi\n\nbraces(block: List[int]) -> string =>\n    \"{{\" \"{block |> join(\", \")}\" \"}}\"\n\nshow(sizes: int[]) is\n    let total = sizes |> reduce(0, (running, size) => running + size)\n    let items = (1::total) |> collect()\n\n    write_line(\"partitions({sizes |> join(\", \")}):\")\n\n    partitions(items, sizes |> collect())\n        |> each(partition =>\n            write_line(\"  ({partition |> map(braces) |> join(\", \")})\"))\nsi\n\nshow([2, 0, 2])",
  "fullSource": "use default\n\ncombinations(items: List[int], k: int) -> Pipe[List[int]] is\n    if k == 0 then\n        yield LIST()\n    elif items.count >= k then\n        let rest = items[1..<0]\n\n        for tail in combinations(rest, k - 1) do\n            let whole = LIST()\n\n            whole.add(items[0])\n\n            for value in tail do\n                whole.add(value)\n            od\n\n            yield whole\n        od\n\n        yield in combinations(rest, k)\n    fi\nsi\n\npartitions(items: List[int], sizes: List[int]) -> Pipe[List[List[int]]] is\n    if sizes.count == 0 then\n        yield LIST()\n    else\n        for block in combinations(items, sizes[0]) do\n            let remaining = items |> except(block) |> collect()\n\n            for tail in partitions(remaining, sizes[1..<0]) do\n                let whole = LIST()\n\n                whole.add(block)\n\n                for rest in tail do\n                    whole.add(rest)\n                od\n\n                yield whole\n            od\n        od\n    fi\nsi\n\nbraces(block: List[int]) -> string =>\n    \"{{\" \"{block |> join(\", \")}\" \"}}\"\n\nshow(sizes: int[]) is\n    let total = sizes |> reduce(0, (running, size) => running + size)\n    let items = (1::total) |> collect()\n\n    write_line(\"partitions({sizes |> join(\", \")}):\")\n\n    partitions(items, sizes |> collect())\n        |> each(partition =>\n            write_line(\"  ({partition |> map(braces) |> join(\", \")})\"))\nsi\n\nshow([2, 0, 2])",
  "output": "partitions(2, 0, 2):\n  ({1, 2}, {}, {3, 4})\n  ({1, 3}, {}, {2, 4})\n  ({1, 4}, {}, {2, 3})\n  ({2, 3}, {}, {1, 4})\n  ({2, 4}, {}, {1, 3})\n  ({3, 4}, {}, {1, 2})\n",
  "images": [],
  "playground": true,
  "playgroundPath": "ordered-partitions",
  "hovers": [],
  "diagnostics": []
}
