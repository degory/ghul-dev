{
  "name": "rosetta-non-continuous-subsequences",
  "code": "use default\n\nlet elements = [1, 2, 3, 4]\n\nselected(mask: int) -> List[int] =>\n    (0..elements.count)\n        |> filter(index => (mask & (1 << index)) != 0)\n        |> map(index => elements[index])\n        |> collect()\n\ncontinuous(chosen: List[int]) -> bool =>\n    chosen[chosen.count - 1] - chosen[0] + 1 == chosen.count\n\n(1..(1 << elements.count))\n    |> map(selected)\n    |> filter(chosen => chosen.count > 1 /\\ !continuous(chosen))\n    |> map(chosen => chosen |> join(\", \"))\n    |> each(line => write_line(line))",
  "fullSource": "use default\n\nlet elements = [1, 2, 3, 4]\n\nselected(mask: int) -> List[int] =>\n    (0..elements.count)\n        |> filter(index => (mask & (1 << index)) != 0)\n        |> map(index => elements[index])\n        |> collect()\n\ncontinuous(chosen: List[int]) -> bool =>\n    chosen[chosen.count - 1] - chosen[0] + 1 == chosen.count\n\n(1..(1 << elements.count))\n    |> map(selected)\n    |> filter(chosen => chosen.count > 1 /\\ !continuous(chosen))\n    |> map(chosen => chosen |> join(\", \"))\n    |> each(line => write_line(line))",
  "output": "1, 3\n1, 4\n2, 4\n1, 2, 4\n1, 3, 4\n",
  "images": [],
  "playground": true,
  "playgroundPath": "non-continuous-subsequences",
  "hovers": [],
  "diagnostics": []
}
