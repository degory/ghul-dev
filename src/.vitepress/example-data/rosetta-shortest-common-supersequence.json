{
  "name": "rosetta-shortest-common-supersequence",
  "code": "use IO.Std.write_line\n\nshortest_supersequence(u: string, v: string) -> string =>\n    if u.length == 0 then\n        v\n    elif v.length == 0 then\n        u\n    elif u[0] == v[0] then\n        \"{u[0]}{shortest_supersequence(u[1..<0], v[1..<0])}\"\n    else\n        (let keep_u = \"{u[0]}{shortest_supersequence(u[1..<0], v)}\"\n         let keep_v = \"{v[0]}{shortest_supersequence(u, v[1..<0])}\"\n\n         if keep_u.length <= keep_v.length then keep_u else keep_v fi)\n    fi\n\nlet u = \"abcbdab\"\nlet v = \"bdcaba\"\n\nwrite_line(\"u = {u}\")\nwrite_line(\"v = {v}\")\nwrite_line(\"s = {shortest_supersequence(u, v)}\")",
  "fullSource": "use IO.Std.write_line\n\nshortest_supersequence(u: string, v: string) -> string =>\n    if u.length == 0 then\n        v\n    elif v.length == 0 then\n        u\n    elif u[0] == v[0] then\n        \"{u[0]}{shortest_supersequence(u[1..<0], v[1..<0])}\"\n    else\n        (let keep_u = \"{u[0]}{shortest_supersequence(u[1..<0], v)}\"\n         let keep_v = \"{v[0]}{shortest_supersequence(u, v[1..<0])}\"\n\n         if keep_u.length <= keep_v.length then keep_u else keep_v fi)\n    fi\n\nlet u = \"abcbdab\"\nlet v = \"bdcaba\"\n\nwrite_line(\"u = {u}\")\nwrite_line(\"v = {v}\")\nwrite_line(\"s = {shortest_supersequence(u, v)}\")",
  "output": "u = abcbdab\nv = bdcaba\ns = abcbdcaba\n",
  "images": [],
  "playground": true,
  "playgroundPath": "shortest-common-supersequence",
  "hovers": [],
  "diagnostics": []
}
