{
  "name": "rosetta-entropy",
  "code": "use default\nuse System.Math\n\nentropy(text: string) -> double is\n    let counts = MAP()\n\n    for character in text do\n        let seen mut = 0\n\n        counts.try_get_value(character, seen ref)\n\n        counts[character] = seen + 1\n    od\n\n    let total = cast double(text.length)\n\n    return counts.values\n        |> map(count => cast(count) / total)\n        |> reduce(0.0D, (bits, probability) =>\n            bits - probability * Math.log2(probability))\nsi\n\nlet text = \"1223334444\"\n\nwrite_line(\"{text}: {entropy(text)} bits/symbol\")",
  "fullSource": "use default\nuse System.Math\n\nentropy(text: string) -> double is\n    let counts = MAP()\n\n    for character in text do\n        let seen mut = 0\n\n        counts.try_get_value(character, seen ref)\n\n        counts[character] = seen + 1\n    od\n\n    let total = cast double(text.length)\n\n    return counts.values\n        |> map(count => cast(count) / total)\n        |> reduce(0.0D, (bits, probability) =>\n            bits - probability * Math.log2(probability))\nsi\n\nlet text = \"1223334444\"\n\nwrite_line(\"{text}: {entropy(text)} bits/symbol\")",
  "output": "1223334444: 1.8464393446710154 bits/symbol\n",
  "images": [],
  "playground": true,
  "playgroundPath": "entropy",
  "hovers": [],
  "diagnostics": []
}
