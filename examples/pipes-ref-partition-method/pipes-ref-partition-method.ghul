use Collections.Iterable;
use Collections.LIST;
use Collections.MAP;
use Collections.SET;
use Ghul.Pipes.INDEXED_VALUE;

trait Pipe[T] is
// >>>
    partition(
        predicate: (T) -> bool pure
    ) -> (LIST[T], LIST[T]) pure;
// <<<
si

entry() is si
