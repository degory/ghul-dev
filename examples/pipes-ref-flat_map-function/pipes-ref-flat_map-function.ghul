use Collections.Iterable;
use Collections.LIST;
use Collections.MAP;
use Collections.SET;
use Ghul.Pipes.INDEXED_VALUE;
use Ghul.Pipes.Pipe;

// >>>
flat_map[T,U](
    source: Iterable[T],
    mapper: (T) -> Iterable[U] pure
) -> Pipe[U] pure;
// <<<

entry() is si
