use IO.Std.write_line;

// >>>
let numbers = [1, 2, 3, 4, 5, 6];

// nothing has asked this chain for elements yet, so peek's
// action has not run
let stages = numbers
    | .peek(x => write_line("  pulled {x}"))
    | .filter(x => x % 2 == 0)
    | .map(x => x * 10);

write_line("chain built - nothing has run yet");

// collect_list is a terminal, so it asks for the elements
let result = stages | .collect_list();

write_line("result: {result | .join(", ")}");
// <<<
