use IO.Std.write_line;

class Animal is si

class Cat(name: string): Animal is
    purr() -> string => "{name} purrs";
si

entry() is
    let a: Animal = Cat("whiskers");
// >>>
    let c = cast Cat(a);

    if c? then
        write_line(c.purr());
    fi
// <<<
si
