use Collections.Iterable
use Collections.LIST
use Collections.MAP
use Collections.SET
use Ghul.Pipes.INDEXED_VALUE
use Ghul.Pipes.Pipe

// >>>
group_by[T,K](
    source: Iterable[T],
    key_selector: (T) -> K pure
) -> MAP[K, LIST[T]] pure
// <<<

entry() is si
