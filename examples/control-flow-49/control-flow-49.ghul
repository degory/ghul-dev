use IO.Std.write_line;

squares(limit: int) -> Collections.Iterable[int] is
// >>>
    let i mut = 1;
    while i <= limit do
        yield i * i;
        i = i + 1;
    od
// <<<
si

entry() is
    for s in squares(4) do
        write_line(s);
    od
si
