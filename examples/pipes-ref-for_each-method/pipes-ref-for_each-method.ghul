use Collections.Iterable;
use Collections.LIST;
use Collections.MAP;
use Collections.SET;
use Ghul.Pipes.INDEXED_VALUE;

trait Pipe[T] is
// >>>
    for_each(action: T -> void) -> void;
// <<<
si

entry() is si
