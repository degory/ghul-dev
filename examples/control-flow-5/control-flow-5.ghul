use IO.Std.write_line;

entry() is
    let list = [1, 2, 3, 4];
// >>>
    if list.count > 0 then
        write_line("list is not empty");
    else
        write_line("list is empty");
    fi
// <<<
si
