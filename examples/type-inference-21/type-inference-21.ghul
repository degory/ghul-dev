// >>>
class Animal is
    init() is si
    speak() -> string => "animal";
si

class Cat: Animal is
    init() is si
si

class Dog: Animal is
    init() is si
si

merge[T](a: T, b: T) -> T => a;
// <<<

entry() is
// >>>
    let a = merge(Cat(), Dog());
// <<<
si
