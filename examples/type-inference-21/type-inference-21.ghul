// >>>
class Animal abstract is
    speak() -> string => "animal";
si

class CAT(): Animal;

class DOG(): Animal;

merge[T](a: T, b: T) -> T => a;
// <<<

entry() is
// >>>
    let a = merge(CAT(), DOG());
// <<<
si
