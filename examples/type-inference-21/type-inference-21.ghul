// >>>
class Animal() is
    speak() -> string => "animal";
si

class Cat(): Animal;

class Dog(): Animal;

merge[T](a: T, b: T) -> T => a;
// <<<

entry() is
// >>>
    let a = merge(Cat(), Dog());
// <<<
si
