// >>>
class Animal() is
    speak() -> string => "animal";
si

class Cat(): Animal is
si

class Dog(): Animal is
si

merge[T](a: T, b: T) -> T => a;
// <<<

entry() is
// >>>
    let a = merge(Cat(), Dog());
// <<<
si
