use IO.Std.write_line;

lookup() -> string? => "Alice";

entry() is
    let name = lookup();
// >>>
    if name? then
        write_line("name is {name}"); // name is non-optional here
    fi
// <<<
si
