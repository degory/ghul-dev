use Collections.Iterable;
use Collections.LIST;
use Collections.MAP;
use Collections.SET;
use Ghul.Pipes.INDEXED_VALUE;

trait Pipe[T] is
// >>>
    first_map[U](
        mapper: (T) -> Ghul.MAYBE[U] pure
    ) -> Ghul.MAYBE[U] pure;
// <<<
si

entry() is si
