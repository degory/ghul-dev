class BOX[T] is
    _value: T;

    init() is
        _value = default;
    si

    set(value: T) is
        _value = value;
    si

    get() -> T => _value;
si

entry() is
// >>>
    // m is BOX[?] here; the type argument is not
    // yet known
    let m = BOX();

    // the set call carries an int, so m is BOX[int]
    m.set(42);

    let x = m.get();
// <<<
si
