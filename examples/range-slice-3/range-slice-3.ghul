use IO.Std.write_line;
use Collections.LIST;
use Ghul.Pipes;

// >>>
let xs = LIST[int]([1, 2, 3, 4]);
let middle = xs[1..3];

write_line("{middle |> join(", ")}");

xs[2] = 99;

write_line("{middle |> join(", ")}");
// <<<
