use Collections.Iterable;
use Collections.LIST;
use Collections.MAP;
use Collections.SET;
use Ghul.Pipes.INDEXED_VALUE;
use Ghul.Pipes.Pipe;

// >>>
take_while[T](
    source: Iterable[T],
    predicate: (T) -> bool pure
) -> Pipe[T] pure;
// <<<

entry() is si
